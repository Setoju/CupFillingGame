<Window x:Class="CupFilling.Level1Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CupFilling"
        mc:Ignorable="d"
        Title="Level1Window" Height="1920" Width="1080" WindowState="Maximized">
    <Grid>
        <StackPanel>
            <TextBlock x:Name="remainingWaterText" Text="{Binding Path=WaterAmount}"/>
        </StackPanel>
        <Button Name="NextLevelButton" Content="Next Level" Height="50" Width="100"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="50" Click="NextLevelButton_Click" />
        <Canvas Name="FirstLevelCanvas" Height="1900" Width="700" Background="LightGray">
            <Rectangle Tag="wall"  Width="100" Height="8" Fill="Gray" Canvas.Left="159" Canvas.Top="664" HorizontalAlignment="Left" VerticalAlignment="Top">
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="45"/>
                </Rectangle.RenderTransform>
            </Rectangle>
            <Rectangle Tag="wall"  Width="100" Height="8" Fill="Gray" Canvas.Left="397" Canvas.Top="478" HorizontalAlignment="Left" VerticalAlignment="Top">
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="135"/>
                </Rectangle.RenderTransform>
            </Rectangle>
        </Canvas>
    </Grid>
</Window>
